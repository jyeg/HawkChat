<!-- Signed in as @<%= cookies.signed[:username] %>. -->
<!-- <%= link_to 'Change', root_path %> -->
<br/>
<br/>

<div id='messages'></div>

<br/>
<br/>

<%= form_for :message, url: messages_path, remote: true, id: 'messages-form' do |f| %>
  <%= f.label :body  %><br/>
  <%= f.text_field :body %><br/>
  <%= f.submit 'Send' %>
<% end %>
<!-- username <%= @username %> -->
<%= react_component 'Messages', username: cookies.signed[:username], messages: render(partial: 'messages/message.json', locals: {messages: @messages})  %>
